using UnityEngine;
using System.Collections;

public class Send_Ball_Up_Script : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
	
	}

/*	void OnMouseOver() {
		if (Input.GetMouseButtonDown (0)) {
			Ball_Spawner.SpawnUp();
			Debug.Log("rrrraaaarrr");
		}
	}
*/			
}